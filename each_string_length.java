import javax.swing.*;
//it will take input form cmd panel and give the number of each word of string
class mytest
{
    public static void main (String[]a)
    {
        int i;
        for(i=0;i<a.length;i++)
        {
            System.out.println("length of "+a[i]+" is "+a[i].length());
        }
    }
}